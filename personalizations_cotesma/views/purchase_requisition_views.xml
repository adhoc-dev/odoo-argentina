<odoo>
    <!-- Form -->
    <record id="view_purchase_requisition_form" model="ir.ui.view">
        <field name="name">purchase.requisition.form</field>
        <field name="model">purchase.requisition</field>
        <field name="inherit_id" ref="purchase_requisition.view_purchase_requisition_form" />
        <field name="arch" type="xml">
            <field name="type_id" position="after">
                <field name="vendor_ids" context="{'res_partner_search_mode': 'supplier'}" attrs="{'invisible':[('is_quantity_copy', '=', 'none')], 'readonly': [('state','!=','draft')]}" widget="many2many_tags"/>
                <field name="purchase_ids" invisible="1"/>
            </field>
            <field name="vendor_id" position="attributes">
                <attribute name="attrs">{'invisible':[('is_quantity_copy', '!=', 'none')], 'required': [('is_quantity_copy', '=', 'none')], 'readonly': [('state', 'in', ['ongoing','done'])]}</attribute>
            </field>
            <div name="button_box">
                <button name="view_purchase_order_lines"  string="Lineas de Compra" type="object" icon="fa-list-alt" attrs="{'invisible': [('purchase_ids', '=', [])]}"/>
            </div>
            <xpath expr="//field[@name='line_ids']/tree//field[@name='product_id']" position="after">
                <field name="name" />
            </xpath>
        </field>
    </record>

    <!-- Kanban -->
    <record id="view_purchase_requisition_kanban" model="ir.ui.view">
        <field name="name">purchase.requisition.kanban</field>
        <field name="model">purchase.requisition</field>
        <field name="inherit_id" ref="purchase_requisition.view_purchase_requisition_kanban" />
        <field name="arch" type="xml">
            <field name="vendor_id" position="replace">
                <field name="vendor_ids"/>
            </field>
        </field>
    </record>

    <act_window name="Send Mail (Purchase Order: Send RFQ)"
        id="action_send_multiple_rfq_mail"
        res_model="mail.compose.message"
        binding_model="purchase.order"
        binding_views="list"
        view_mode="form"
        target="new"
        context="{
            'default_composition_mode': 'mass_mail',
            'default_use_template': True,
            'default_template_id': ref('purchase.email_template_edi_purchase'),
            'mark_rfq_sent': True,
        }"
    />

</odoo>
