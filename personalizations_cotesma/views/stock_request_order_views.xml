<odoo>
  <!-- Form -->
  <record id="stock_request_order_form" model="ir.ui.view">
    <field name="name">stock.request.order.form</field>
    <field name="model">stock.request.order</field>
    <field name="inherit_id" ref="stock_request.stock_request_order_form" />
    <field name="arch" type="xml">
      <field name="warehouse_id" position="after">
        <field name="warehouse_code" invisible="1"/>
        <field name="user_tag_allow_ids" invisible="1" />
      </field>
      <field name="product_id" position="attributes">
        <attribute name="domain">[('tag_ids', 'in', parent.user_tag_allow_ids)]</attribute>
      </field>
      <!-- This personalization cancel the behaviour of the setting "Allow all Locations Types" define by the boolean stock_request_allow_virtual_loc -->
      <field name="location_id" position="attributes">
        <attribute name="domain">
          ['&amp;',('location_id.complete_name', 'like', warehouse_code),("usage", "in", ["internal", "transit"])]
        </attribute>
      </field>
      <field name="stock_request_ids" position="before">
        <button name="add_products_to_stock_request" type="object" string="Search Products" states="draft,sent"/>
      </field>
    </field>
  </record>
  <record id="stock_request.menu_stock_request" model="ir.ui.menu">
    <field name="groups_id" eval="[(6,0,[ref('base.group_no_one')])]" />
  </record>
</odoo>
